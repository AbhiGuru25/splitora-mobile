{
    "buildCommand": "node_modules/.bin/expo export -p web --output-dir dist",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}